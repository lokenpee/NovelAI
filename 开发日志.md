# 开发日志

## 2026-08-31 22:12（Asia/Shanghai）

- **执行事项**：重构并优化 `PRD.md`，保留 `PRD - 副本.md` 作为原始需求备份。
- **涉及模块**：ProjectStore、TextPreprocessor、CategoryRegistry、WorldbookPipeline、WorldbookEditor、BeatPipeline、RuntimeStateMachine、DirectorAdapter、ActorPromptBuilder、ExecutionInjector、ApiRouter、SettingsStore、SillyTavernAdapter。
- **涉及函数/方法契约**：`loadProject`、`saveProject`、`exportProject`、`importProject`、`startAtChapter`、`nextBeat`、`previousBeat`、`commitActorTurn`、SillyTavern `generate_interceptor`、`getContext`、`generateRaw`、`generateQuietPrompt`、`saveMetadata`、`writeExtensionField`。
- **涉及关键变量/字段**：`stage_idx`、`director_index`、`isNewBeat`、`isNewChapter`、`conflict_level`、`will_complete_this_turn`、`action_chain`、`schemaVersion`、`processing`。
- **主要结果**：按 Problem Statement / Solution / User Stories / Implementation Decisions / Testing Decisions / Out of Scope / Further Notes 结构重写；明确 1 基坐标、数据契约、模块边界、模型失败回退、SillyTavern 拦截器约束、敏感配置存储和可验收测试范围。
- **验证**：确认 `PRD.md` 与原始副本分离，文件为 UTF-8（无 BOM），章节标题和必需 PRD 段落齐全。
- **补充**：补回默认章节正则、章节/节拍首尾片段规则、人物/地点默认字段、世界书导出映射和导演/演员路由设置契约。

## 2026-08-31 22:33（Asia/Shanghai）

- **执行事项**：根据用户反馈，在还原后的 `PRD .md` 原文基础上进行“只增补、不删减”的技术串联与结构整理；未使用 `to-prd` skill。
- **涉及模块**：文件导入、小说清洗、章节捕获、世界书抽取/生成结果、导演解析节拍、故事大纲、当前章节概览、开场白、设置、导演—演员运行时。
- **涉及函数/方法**：`loadProject`、`saveProject`、`previewClean`、`applyClean`、`captureChapters`、`updateChapter`、`mergeChapter`、`deleteChapter`、`buildCategorySchema`、`buildExtractionPrompt`、`extractChapter`、`normalizeEntry`、`mergeEntry`、`parseChapterBeats`、`deriveBoundaryAssets`、`setStage`、`startFromChapter`、`moveBeat`、`commitActorTurn`、`buildDirectorPrompt`、`callDirector`、`normalizeDirectorDecision`、`buildActorExecution`、`interceptGeneration`、`removeInjectedExecution`、`injectExecution`。
- **涉及关键变量/字段**：`projectState`、`projectId`、`chapterId`、`beatId`、`currentStage`、`stage_idx`、`isNewbeat`、`isNewchapter`、`director_index`、`processing`、`START_ANCHOR`、`RECENT_USER_INPUT`、`RECENT_ASSISTANT`、`DYNAMIC_JSON_TEMPLATE`、`ENABLED_CATEGORY_NAMES`。
- **主要结果**：保留原 PRD 的全部 811 行内容和所有提示词，在其间新增 266 行技术补充；明确项目唯一数据源、任务版本、状态事务、提示词变量来源、SillyTavern manifest/拦截器/事件时序、导演响应校验、注入消息清理、演员成功提交和端到端验收清单。
- **验证**：逐行检查原始副本内容在 `PRD .md` 中按原顺序完整存在（缺失 0 行）；关键提示词标记数量与原文一致；确认 `PRD .md` 与 `PRD - 副本.md` 文件名差异已保留，未擅自重命名。

## 2026-08-31 22:42（Asia/Shanghai）

- **执行事项**：根据用户反馈重新调整 PRD 阅读顺序，移除开头的英文变量/方法堆叠，改为先讲业务流程；技术名词改为在首次使用处解释，并在文末提供汇总表。
- **涉及章节**：文档总流程、概念定义、文件上传、世界书抽取、导演解析节拍、故事坐标、开场白、设置、导演—演员运行时、端到端验收。
- **涉及变量/方法**：`projectId`、`chapterId`、`beatId`、`projectState`、`sourceText`、`chapters`、`categoryConfigs`、`worldbookEntries`、`beatAssets`、`processing`、`schemaVersion`、`stage_idx`；对应方法说明集中到首次使用处和文末技术名词汇总。
- **主要结果**：删除“先记代码再读需求”的 0.2/0.3 结构，新增易读的流程表、依赖条件和第 6 节技术名词索引；原始提示词及 811 行原文仍保持完整。
- **验证**：重新执行原文逐行子序列检查，缺失 0 行；当前 PRD 共 1091 行，技术补充 16 处，提示词标记数量与原始副本一致。

## 2026-08-31 23:08（Asia/Shanghai）

- **执行事项**：按用户对章节/节拍标识的定义，修正 `PRD .md` 中所有相关表述。
- **涉及变量**：`chapterId`、`chapterName`、`beatId`、`stage_idx`、`currentStage`、`chapterIndex`、`beatIndex`。
- **修正内容**：`chapterId` 改为章节队列中的唯一编号；`chapterName` 改为正则捕获出的唯一章节名称并可作为章节键；`beatId` 改为当前节拍集合按顺序生成的编号，不再声称节拍重排后可追踪旧资产；补充节拍删减/合并/重排后重新编号及当前坐标失效处理。
- **验证**：搜索并检查所有 `chapterId`、`chapterName`、`beatId` 相关段落，确认不存在“标题不能作为唯一键”或“旧 beatId 仍能找到原资产”的旧表述。

## 2026-08-31 23:10（Asia/Shanghai）

- **执行事项**：根据用户进一步澄清，修正章节和节拍标识的生命周期定义，并同步修改所有关联段落。
- **涉及变量**：`chapterId`、`chapterName`、`beatId`、`stage_idx`、`currentStage`、`isNewbeat`、`isNewchapter`。
- **修正内容**：明确 `chapterId` 为每章唯一的正整数编号；明确 `chapterName` 为正则捕获出的唯一章节名称，可作为章节键；删除“标题可能重复”的错误假设；明确 `beatId` 只属于当前节拍集合，用户删减/合并/重排后重新编号，旧节拍资产不再由旧编号追踪；当前坐标引用失效时必须要求用户重新选择。
- **验证**：原始副本 811 行在当前 PRD 中仍全部存在；旧的冲突表述搜索结果均为 false；相关标识定义、节拍修改规则、运行时坐标和验收清单已同步。
## 2026-08-31

- 初始化开发计划：新增 `PLAN.md`，梳理 PRD 四个界面、领域/服务/UI 分层、SillyTavern 适配、运行时链路、测试与交付阶段；记录 `projectState`、`schemaVersion`、`currentStage`、`generate_interceptor` 等核心契约及待确认边界。
- 开始阶段 0/1 与基础工作台实现：新增 `manifest.json`、`package.json`、`index.js`，以及 `src/domain`、`src/services`、`src/ui`、`src/styles` 模块。实现 `createProject`、`validateProject`、`migrateProject`、`previewClean`、`applyClean`、`captureChapters`、`normalizeEntry`、`mergeEntry`、`parseExtractionResponse`、`normalizeBeats`、`setStage`、`commitActorTurn`、`ApiRouter`、`ProjectStore`、`RuntimeController` 和工作台 UI。API Key 仅保存在 `SettingsStore` 内存，日志过滤敏感字段。
- 完成第二轮一致性修正：章节捕获成功后标记为已确认；节拍任务跳过已完成章节并支持单章重 roll；章节合并会使受影响节拍失效；演员成功提交时记录开场白发送坐标；运行时截取最近两句并要求存在世界书；角色卡导出同时尝试通过 `writeExtensionField` 写入当前角色扩展；新增 README、导出/API 路由测试。

## 2026-08-31 23:59（Asia/Shanghai）

- **执行事项**：完成 StoryWeaver 形式兼容和末轮实现校验。
- **涉及文件**：`manifest.json`、`drawer-component.html`、`index.js`、`src/ui/panel.js`、`src/styles/index.css`、`src/domain/beats/mutateBeats.js`、`src/services/runtime/runtimeController.js`、`src/services/jobs/*`、`tests/domain.test.js`、`README.md`。
- **涉及函数/变量**：`onActivate`、`initializeAsync`、`toggleDrawer`、`updateBeatAsset`、`runWorldbookJob`、`runBeatJob`、`commitActorTurn`、`openingSentForStage`、`currentStage`、`generate_interceptor`。
- **主要结果**：使用 StoryWeaver 同类的 `manifest.json + drawer-component.html` 启动器和抽屉工作台；V2 角色卡导出映射保留世界书位置、深度与顺序；补全节拍编辑边界校验、逐章任务恢复和导演执行单注入/提交。
- **验证**：`npm test` 共 13 项通过；所有 JS 通过 `node --check`；模拟 SillyTavern 上下文导入入口成功。

## 2026-09-01 00:04（Asia/Shanghai）

- **执行事项**：按本机 SillyTavern 1.14.0 源码校正消息事件与模板挂载适配。
- **涉及函数/变量**：`MESSAGE_SENT(messageId)`、`MESSAGE_RECEIVED(messageId, type)`、`onMessageReceived`、`mountDrawerTemplate`、`invalidateChapterAssets`、`sourceVersion`。
- **主要结果**：事件处理改为使用聊天数组按消息 ID 取正文，确保演员成功消息能够提交节拍；抽屉模板使用 `renderExtensionTemplateAsync` 并插入扩展按钮旁，缺失模板时仍回退到设置页；章节编辑/删除/合并会清理受影响节拍并使相关世界书任务重新处理。
- **验证**：新增运行时 messageId、章节失效和源版本覆盖测试；`npm test` 共 14 项通过。

## 2026-09-01 00:11（Asia/Shanghai）

- **执行事项**：扩充 PRD 默认提示词和工作台细节，完成任务取消链路。
- **涉及函数/变量**：`DEFAULT_MAIN_PROMPT`、`DEFAULT_DIRECTOR_PROMPT`、`DEFAULT_ACTOR_PROMPT`、`buildDirectorPrompt`、`runWorldbookJob`、`runBeatJob`、`cancelActiveJob`、`autoIncrementOrder`、`lastWorldbookAt`。
- **主要结果**：默认人物/地点、导演/演员提示词保留 PRD 的字段和硬规则；新增章节浏览、类别顺序递增、合并目标分类、任务取消按钮；导出角色卡写入项目标识以供运行时校验。
- **验证**：新增取消队列测试；`npm test` 共 16 项通过。

## 2026-09-01 00:14（Asia/Shanghai）

- **执行事项**：修正独立世界书导出为 SillyTavern 1.14.0 原生字段，并补齐扩展停用清理。
- **涉及函数/变量**：`toSillyTavernWorldbook`、`toCharacterCard`、`onDisable`、`bindProjectMetadataSync`、`disposers`、`metadataUnsubscribe`。
- **主要结果**：独立世界书导出使用 `uid/key/keysecondary/position(number)`；角色卡 V2 导出使用 `keys/secondary_keys`；关闭扩展时移除事件监听、停止元数据同步并卸载抽屉 UI。
- **验证**：参照本机 `world-info.js` 与 `characters.js` 导入转换字段逐项映射；生命周期模拟和 `npm test` 共 16 项通过。

## 2026-09-01 00:18（Asia/Shanghai）

- **执行事项**：补齐生成结果和节拍概览的人机操作链路。
- **涉及函数/变量**：`renderWorldbook`、`mergeAdjacentBeatAsset`、`moveBeatAsset`、`confirmChapter`、`activeJobController`、`currentStage`。
- **主要结果**：生成结果改为按类别分组；节拍支持摘要编辑、相邻合并、重排和边界校验；章节编辑后可显式确认再送入 LLM；总开关关闭时禁止启动模型任务。
- **验证**：新增节拍合并/重排测试；`npm test` 共 17 项通过，所有 JS 语法检查通过。

## 2026-09-01 00:20（Asia/Shanghai）

- **执行事项**：完善工程包迁移和故事坐标恢复边界。
- **涉及函数/变量**：`migrateProject`、`validateStage`、`capture`、`sourceVersion`、`currentStage`。
- **主要结果**：旧工程包缺失可选资产时恢复为空资产/待处理状态而非伪造完成；恢复聊天坐标前验证节拍是否仍存在；章节捕获不再无故递增正文版本。
- **验证**：新增工程包迁移测试；`npm test` 共 18 项通过，所有 JS 语法检查通过。

## 2026-09-01 00:22（Asia/Shanghai）

- **执行事项**：交付前烟雾验证。
- **验证**：使用根目录 `回到明朝当王爷（删减）.txt` 和默认章节正则，成功捕获 5 章（第一章“九世善人”至第五章“相濡以沫”）；检查 `manifest.json` 的 JS/CSS/HTML 资源、全局 `novelAiGenerateInterceptor` 与 `onActivate` 生命周期导出均存在；`npm test` 共 18 项通过。

## 2026-09-01 00:35（Asia/Shanghai）

- **执行事项**：按 StoryWeaver API 服务的容错思路增强 `ApiRouter`，并核对持久化边界。
- **涉及文件/方法**：`ApiRouter.generate`、`generateCustom`、`retryPolicy.withRateLimitRetry`、`responseParsers.readSseResponse`、`ProjectStore`、`SettingsStore`。
- **主要结果**：新增 429/5xx/网络临时失败重试、`Retry-After` 支持、请求超时、OpenAI/Anthropic SSE 解析、流式失败回退非流式、JSON Schema 不兼容自动降级；增加超时、重试和流式开关设置并持久化。章节、世界书、节拍和非敏感设置继续跨关闭/刷新恢复；API Key 仍仅保留在当前页面内存，未写入任何持久化介质。
- **验证**：新增限流重试、SSE 文本/思考提取、JSON Schema 降级、跨 `ProjectStore` 实例恢复测试；`npm test` 共 22 项通过，所有 JS 语法检查和入口生命周期模拟通过。

## 2026-09-01 00:36（Asia/Shanghai）

- **执行事项**：补齐 API 思考内容的内存读取接口。
- **涉及方法/变量**：`ApiRouter.lastReasoning`、`ApiRouter.getLastReasoning`、`extractReasoning`。
- **主要结果**：OpenAI、Anthropic、Gemini 兼容响应及流式响应中可识别的 reasoning/thinking 内容会随本次 API 调用保存在对应角色的运行时内存；不写入设置、项目包或日志。
- **验证**：JSON Schema 自动降级测试同时验证 reasoning 提取；`npm test` 共 22 项通过。

## 2026-09-01 00:44（Asia/Shanghai）

- **执行事项**：按用户明确要求，将 API Key 的持久化策略调整为与 StoryWeaver 一致。
- **涉及文件/方法**：`SettingsStore.load`、`SettingsStore.save`、`SettingsStore.setApiKey`、`NovelAiPanel.saveSettings`、设置页 API Key 输入框。
- **主要结果**：演员和导演 API Key 写入 `extensionSettings.novelai.actor.apiKey` / `extensionSettings.novelai.director.apiKey`，刷新页面或关闭抽屉后会恢复；Key 仍不会写入工程包、角色卡、日志或 IndexedDB，且不需要修改 `D:\AI\SillyTavern`。
- **验证**：新增跨 `SettingsStore` 实例恢复 API Key 测试；`npm test` 共 23 项通过，所有 JS 语法检查和入口生命周期模拟通过。

## 2026-09-01 00:48（Asia/Shanghai）

- **执行事项**：按用户要求更新抽屉启动图标。
- **涉及文件/变量**：`drawer-component.html`、`#novelai-icon`。
- **主要结果**：将与 StoryWeaver 重复的 `fa-book-open` 替换为 `fa-clapperboard`，更清晰表达 NovelAI 的故事导演功能。
