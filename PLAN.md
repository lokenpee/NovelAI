# NovelAI 插件开发计划

## 当前目标

按 `PRD .md` 实现一个可安装的 SillyTavern 扩展：TXT 清洗与章节捕获、世界书抽取与编辑、节拍解析与故事坐标、设置页，以及导演—演员运行时拦截链路。

## 约束与设计原则

- 以 `projectState` 作为唯一业务数据源；页面只通过服务/状态模块读写。
- 领域纯逻辑放在 `src/domain`，平台适配放在 `src/services`/`src/adapters`，UI 放在 `src/ui`，样式集中在 `src/styles`。
- 入口 `index.js` 只负责组装、生命周期注册和全局拦截器注册。
- API Key 仅保存在当前会话内存，不写入 `extensionSettings`、工程包、角色卡或日志。
- 所有异步任务携带 `projectId` 与版本号；旧结果丢弃，章节级失败不影响其他章节。
- 每次修改都在根目录 `开发日志.md` 记录时间、函数/方法/变量和变更摘要。

## 分阶段实施

### 阶段 0：基线与契约（先行）

1. 确定扩展根目录、manifest 字段、目标 SillyTavern 生命周期和 `generate_interceptor` 契约。
2. 建立模块目录、测试运行方式和统一日志/错误模型。
3. 定义 `schemaVersion`、`projectState`、`currentStage`、任务状态枚举及迁移入口。

### 阶段 1：领域模型与持久化

1. 实现项目快照创建、校验、版本递增、导入迁移、导出校验。
2. 实现章节/节拍/世界书条目/处理进度的数据结构与不可变更新辅助函数。
3. 使用 localforage（或等价适配器）保存大体积项目数据；使用 SillyTavern `extensionSettings` 仅保存轻量设置。

### 阶段 2：TXT 工作台（界面 1）

1. TXT/工程包拖拽与文件选择、编码/大小/JSON 结构检查。
2. 精确片段清洗：`previewClean`、`applyClean`、撤销及版本失效提示。
3. 默认/自定义正则章节捕获：标题唯一性、索引、空章和预览校验。
4. 章节列表、详情编辑、保存、删除、多选删除、相邻合并和字数统计。

### 阶段 3：世界书抽取与生成结果

1. 类别配置编辑（角色、地点、自定义类别）、字段白名单和动态 JSON Schema。
2. 原始主提示词/类别提示词预览与变量替换。
3. 统一 `ApiRouter`（酒馆 API、OpenAI 兼容、Gemini、Anthropic）及取消/超时/并发信号量。
4. LLM 返回清洗、JSON 提取、Schema 校验、字段过滤、名称规范化、关键词与 Markdown 清洗。
5. `mergeEntry` 确定性跨章节归并、逐章进度持久化、失败重试/取消恢复。
6. 生成结果折叠列表、条目编辑/蓝绿灯/位置深度顺序设置、手动合并、AI 整理、删除确认。
7. 世界书导出与角色卡挂载；无有效角色卡时导出独立世界书并提示。

### 阶段 4：节拍解析、大纲与章节概览（界面 2/3）

1. 按章节队列串行/并发解析节拍，校验至少两个节拍及首尾边界。
2. 生成章节/节拍 opening、ending 资产；重 roll 通过校验后原子替换旧版本。
3. 故事大纲页：待生成/处理中/已生成/失败状态、摘要、重 roll、查看概览、从本章开始。
4. 当前章节概览：节拍折叠查看、编辑/删除/重排/合并、浏览与推进边界。
5. 实现 `currentStage`、`setStage`、`startFromChapter`、`moveBeat`、`isNewbeat`、`isNewchapter` 与聊天元数据恢复。

### 阶段 5：导演—演员运行时

1. SillyTavern 适配器：`getContext`、事件监听、生命周期、聊天元数据和角色卡扩展字段。
2. 运行时前置检查、同聊天锁、最近用户/演员尾部提取、起笔锚点判定。
3. 导演提示词构建、导演路由调用、JSON 提取/规范化/安全回退和本地 `director_index`。
4. 执行单构建、带唯一标记的临时 system 消息清理与原地注入。
5. `MESSAGE_RECEIVED`/`GENERATION_ENDED` 提交回合、开场白一次性状态、节拍完成事务推进。
6. 关闭总开关、缺少素材、非法响应、失败/取消时保持原生 SillyTavern 行为。

### 阶段 6：验证与交付

1. 领域单元测试：清洗、章节捕获、规范化合并、节拍边界、坐标状态机、导演响应校验。
2. 适配器契约测试：manifest、拦截器参数、生命周期事件、`generateRaw`/`generateQuietPrompt`、角色卡扩展字段。
3. 按 PRD 5.1–5.5 验收清单执行手工/自动验证，记录失败案例。
4. 完善 README、安装说明、已知兼容性和开发日志，生成可直接放入 `public/extensions/third-party` 的目录。

## 默认实现假设（待确认）

1. `D:\mic\NovelAI` 作为插件根目录，直接放置 `manifest.json` 与入口文件；不再嵌套第二层扩展目录。
2. 首个可运行版本优先覆盖 Chromium + SillyTavern staging/release 通用 API；对未在本地技术文档声明的世界书写入 API 使用角色卡 `extensions` 字段与标准导出文件兜底。
3. 业务界面使用原生 HTML/CSS/JS 与 SillyTavern 模态框，不引入构建工具；这样可直接加载并便于调试。
4. 没有 API Key 时允许使用酒馆当前连接；自定义 API 的 Key 只保存在内存，刷新后由用户重新输入。

## 需要用户确认的边界

- 是否确认以上插件根目录与“原生 JS、无构建工具”的交付方式？
- 角色卡“挂载世界书”是否接受：导出标准 SillyTavern 世界书 JSON，并将扩展数据写入当前角色卡 `extensions`；还是必须自动调用某个特定 ST 版本的世界书绑定接口？
- 默认的 AI 路由/模型是否有指定服务（例如本地 OpenAI 兼容端点），还是按 PRD 提供的三类协议全部实现并由用户填写？

